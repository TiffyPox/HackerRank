#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
#include <iterator>

int main() 
{
    int numOfArrays = 0;
    int numsOfQueries = 0;

    std::cin >> numOfArrays >> numsOfQueries;
    
    std::cout << numOfArrays << " :: " << numsOfQueries << std::endl;

    // to do: store the arrays
    
    return 0;
}